{
  "name": "Site Check",
  "description": "Checks if a website responds.",
  "icon": "WEB",
  "config_required": "optional",
  "settings": [
    {
      "key": "SITE_URL",
      "label": "URL",
      "type": "url",
      "default": "https://www.google.com",
      "required": true
    },
    {
      "key": "SITE_TIMEOUT",
      "label": "Timeout",
      "type": "slider",
      "default": "5",
      "min": 1,
      "max": 60,
      "step": 1,
      "unit": "s"
    },
    {
      "key": "SITE_HEADERS",
      "label": "Custom headers",
      "type": "keyvalue",
      "default": "",
      "help": "Optional HTTP headers (e.g. Authorization, User-Agent)."
    }
  ]
}
