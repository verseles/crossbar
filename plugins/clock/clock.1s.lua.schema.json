{
  "name": "Clock",
  "description": "Simple clock output.",
  "icon": "CLOCK",
  "config_required": "optional",
  "settings": [
    {
      "key": "CLOCK_FORMAT",
      "label": "Time format",
      "type": "text",
      "default": "HH:mm:ss",
      "placeholder": "HH:mm:ss",
      "help": "Uses crossbar.time() format tokens."
    }
  ]
}
