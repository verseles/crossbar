{
  "name": "CPU Monitor (Lua)",
  "description": "Shows CPU usage with thresholds and optional details.",
  "icon": "CPU",
  "config_required": "optional",
  "settings": [
    {
      "key": "CPU_WARN",
      "label": "Warning threshold",
      "type": "slider",
      "default": "60",
      "min": 1,
      "max": 100,
      "step": 1,
      "unit": "%",
      "help": "Usage >= warn uses yellow"
    },
    {
      "key": "CPU_CRIT",
      "label": "Critical threshold",
      "type": "slider",
      "default": "80",
      "min": 1,
      "max": 100,
      "step": 1,
      "unit": "%",
      "help": "Usage >= crit uses red"
    },
    {
      "key": "CPU_SHOW_PLATFORM",
      "label": "Show platform",
      "type": "checkbox",
      "default": "true"
    },
    {
      "key": "CPU_SHOW_RAW",
      "label": "Show 1-decimal value",
      "type": "checkbox",
      "default": "false"
    }
  ]
}
