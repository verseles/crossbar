{
  "name": "Git Status",
  "description": "Shows current repository status.",
  "icon": "GIT",
  "config_required": "optional",
  "settings": [
    {
      "key": "GIT_SHOW_FILES",
      "label": "Show changed files",
      "type": "switch",
      "default": "true"
    },
    {
      "key": "GIT_MAX_FILES",
      "label": "Max files",
      "type": "slider",
      "default": "10",
      "min": 1,
      "max": 200,
      "step": 1
    }
  ]
}
